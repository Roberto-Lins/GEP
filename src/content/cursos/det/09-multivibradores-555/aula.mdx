---
titulo: "Multivibradores com 555 — aula"
secao: "aula"
secaoOrdem: 2
---

## O que é um multivibrador

Um **multivibrador** é um circuito que gera **onda retangular**. O CI **555** é o multivibrador mais
famoso — e, por dentro, ele nada mais é que **dois comparadores** (do tópico anterior) vigiando a tensão
de um capacitor em **⅓ VCC** e **⅔ VCC**, mais um flip-flop que comuta a saída. Entender isso desmistifica
todas as fórmulas: elas só descrevem o **tempo de carga/descarga** desse capacitor entre esses dois
limiares. É um tópico de **prioridade máxima**.

## Classifique primeiro

- **Astável** — oscila sozinho, sem disparo. É um oscilador de onda retangular contínua.
- **Monoestável** — gera **um único pulso** de duração finita após um disparo, e volta ao repouso.
- **Biestável** — tem **dois estados estáveis** (funciona como memória/flip-flop).

## 555 astável

![555 astável: RA da alimentação ao pino 7, RB até o nó dos pinos 6 e 2, e o capacitor C ao terra.](/imagens/cursos/det/09-555-astavel.png)

O capacitor carrega por `RA + RB` (saída alta) e descarrega por `RB` (saída baixa), oscilando entre ⅓ e
⅔ de VCC. Daí saem os tempos:

- `T_ALTO = 0,7 · (RA + RB) · C`  (carga)
- `T_BAIXO = 0,7 · RB · C`  (descarga)
- `T = T_ALTO + T_BAIXO`
- `f = 1 / T`

![A tensão no capacitor sobe (carga) e desce (descarga) entre ⅓ e ⅔ de VCC; a saída fica alta na carga e baixa na descarga.](/imagens/cursos/det/09-555-ondas.png)

## 555 monoestável

Com apenas `RA` e `C`, o 555 gera **um pulso** após o disparo:

- `LP = 1,1 · RA · C`  (largura do pulso)

<Calc555 />

## 555 biestável

No modo **biestável**, o 555 vira um **flip-flop**: tem dois estados estáveis e **fica em cada um até
receber um comando**. Não há capacitor definindo o tempo — o estado só muda quando uma chave aciona o
**disparo (pino 2)** ou o **reset (pino 4)**. É uma "memória" de 1 bit: a saída permanece onde está até
o próximo acionamento.

## Exemplos resolvidos

> **Astável (slide):** com os valores do slide, `T = 1,6 ms` → `f = 1/T = 1/0,0016 = `**`625 Hz`**.

> **Monoestável (slide):** `RA = 7,5 kΩ`, `C = 0,1 µF` →
> `LP = 1,1 · 7 500 · 0,1×10⁻⁶ = `**`0,83 ms`**. (Converta `kΩ` e `µF` antes!)

> **Astável (Lista 1 — Q3):** resultado oficial `f = `**`659 Hz`** (Lista 2).

## Exercícios dos slides (resolvidos)

#### Exercício nº 1 — astável

![Exercício 1: 555 astável — +5 V, RA = 15 kΩ, RB = 4,7 kΩ, C = 2,2 nF.](/imagens/cursos/det/multivibradores/ex1-astavel.png)

**Dados:** `+5 V`, `RA = 15 kΩ`, `RB = 4,7 kΩ`, `C = 2,2 nF` (0,01 µF no pino 5 = controle).
1. `T_ALTO = 0,7·(RA+RB)·C = 0,7·19,7k·2,2n ≈ `**`30,3 µs`**.
2. `T_BAIXO = 0,7·RB·C = 0,7·4,7k·2,2n ≈ `**`7,2 µs`**.
3. `T = 37,5 µs → f = 1/T ≈ `**`26,6 kHz`** (duty ≈ 81%).
4. **Saída:** onda retangular de **0 a +5 V** (níveis ≈ 0 e VCC).

#### Exercício nº 2 — monoestável

![Exercício 2: 555 monoestável — +10 V, R = 1,2 kΩ, C = 1200 µF, disparo por S1 em t = 3 s.](/imagens/cursos/det/multivibradores/ex2-monoestavel.png)

**Dados:** `+10 V`, `R = 1,2 kΩ` (temporização), `C = 1200 µF`, `S1` (NA) no disparo (pino 2),
pressionada em `t = 3 s` (o 10 kΩ é o pull-up do disparo).
1. `LP = 1,1·R·C = 1,1·1,2k·1200µF ≈ 1,1·1,44 s = `**`1,6 s`**.
2. Em repouso a saída fica em 0. Ao pressionar S1 em `t = 3 s`, a saída sobe para **+10 V** por `LP ≈ 1,6 s`.
3. **Saída:** um único pulso de ≈ 1,6 s — alto de **t = 3 s a ≈ 4,6 s**, depois volta a 0.

#### Exercício nº 3 — biestável

![Exercício 3: 555 biestável — chave S pressionada em 3 s, 6 s, 7 s e 10 s; saída inicial em nível baixo.](/imagens/cursos/det/multivibradores/ex3-biestavel.png)

**Dados:** 555 **biestável**; estado inicial da saída (pino 3) **baixo**; chave `S` pressionada em
`3 s, 6 s, 7 s e 10 s`.

No biestável a saída **troca de estado a cada acionamento** (memória). Partindo do nível baixo:

| Instante | Ação | Saída (pino 3) |
| --- | --- | --- |
| 0–3 s | — | baixo (0 V) |
| 3 s | 1º toque | **alto** (+VCC) |
| 6 s | 2º toque | baixo |
| 7 s | 3º toque | **alto** |
| 10 s | 4º toque | baixo |

**Saída:** 0 V até 3 s; +VCC de 3 a 6 s; 0 V de 6 a 7 s; +VCC de 7 a 10 s; 0 V após 10 s. *(Assume que
cada toque alterna o estado; confirme na figura como S liga aos pinos de disparo/reset.)*

<BlocoConceito titulo="Sinal de que você dominou">
Você recebe `RA`, `RB` e `C` e entrega `T_ALTO`, `T_BAIXO`, `T` e `f` sem travar — e desenha tanto a
saída quanto a tensão no capacitor.
</BlocoConceito>

## Exemplo resolvido — dois 555 (monoestável habilita astável) (SOPA 2021)

### Enunciado
Dois CI 555: o primeiro em **monoestável** e o segundo em **astável**, com a saída do monoestável
**habilitando** o astável. Descreva o comportamento da saída e o intervalo em que o astável oscila.

### O que a questão pede?
Entender a **interação** entre os dois 555 — quando o oscilador (astável) está ligado.

### Ideia central
O **monoestável** cria uma **janela de tempo** (um pulso de largura `LP = 1,1·R·C`). O **astável** é um
oscilador que só funciona enquanto está **habilitado** (pelo pino RESET). Ligando a saída do monoestável
ao RESET do astável, o astável **só oscila durante o pulso do monoestável**.

### Passo a passo
1. Disparado em `t = 0`, o monoestável fica em nível alto por `LP = 1,1·R·C`.
2. Esse nível alto **habilita** o astável (tira o RESET).
3. Durante a janela, o astável oscila com `T = T_ALTO + T_BAIXO` (suas próprias fórmulas).
4. Terminado o pulso do monoestável, o astável é **desabilitado** → saída em repouso.

### Resposta
A saída do astável aparece como uma rajada de pulsos **apenas dentro da janela do monoestável** — na
SOPA, aproximadamente de **2 s a 7 s** (gabarito oficial).

### Pegadinha da questão
O astável **não oscila o tempo todo**: ele depende da habilitação do monoestável. Calcule a janela pelo
monoestável (`1,1·R·C`) e a frequência interna pelo astável (`0,7…`).

### Como reconhecer na prova
Dois 555 em cascata, saída de um no RESET do outro = **um define a janela, o outro oscila dentro dela**.

## Para onde vamos

Note como tudo aqui gira em torno de **comparar a tensão do capacitor com limiares**. No próximo tópico,
o **Schmitt-trigger**, levamos a comparação a um novo nível: dois limiares criados pela própria saída,
gerando histerese.
